# 项目业务复杂代码生成需求 201-250

<br />

# wv2-12661 健康运动数据平台

请从健康运动数据平台的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
健康运动数据平台要把设备数据、训练计划、运动记录、异常提醒和教练干预连起来。系统服务用户、教练、健康顾问和运营，核心对象包括设备授权、运动轨迹、心率、睡眠、训练计划、目标和健康提醒。

1. 用户绑定手环、手表或 Web 平台，后端统一步数、心率、睡眠、运动轨迹和设备来源，过滤重复采样。
2. 用户设置减脂、增肌、跑步或康复目标，系统按年龄、基线、运动史和禁忌生成训练计划。
3. 运动完成后，后端解析配速、心率区间、消耗、轨迹和恢复建议，前端展示训练效果和风险提示。
4. 教练在授权范围内查看用户趋势，调整训练强度、休息日和课程建议，调整记录同步用户端。
5. 心率异常、过度训练或连续未完成计划会生成提醒，严重风险要求用户确认或联系紧急人。
6. 运营统计计划完成、设备同步、活跃留存、课程转化和异常指标。
7. 异常覆盖设备断连、心率异常、轨迹漂移、过度训练、隐私撤权和数据重复。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12662 在线约球平台

请从在线约球平台的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
在线约球平台要围绕场馆时段、球局发起、成员报名、AA 支付、到场核销和信用评价展开。系统服务用户、场馆、组织者、客服和运营，核心对象包括场地、时段、球局、成员、候补、费用分摊、核销和信用分。

1. 场馆维护球场、项目、时段、价格、照明和可约人数，后端校验场地容量和营业时间。
2. 用户发起约球局，选择项目、等级、人数、AA 规则、候补要求和取消截止时间。
3. 成员报名后，系统锁定名额、收取订金或场地费，并生成群内通知和候补队列。
4. 开局前系统按人数判断是否成局，不足则自动取消、延长招募或转让场地。
5. 到场扫码核销后，系统记录出勤、爽约、费用分摊和场地使用状态。
6. 赛后记录比分、评价和信用分，影响后续匹配、报名优先级和黑名单。
7. 异常覆盖人数不足、临时退场、场地冲突、订金退款、恶意占位和等级不匹配。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12663 宠物医院管理系统

请从宠物医院管理系统的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
宠物医院管理系统要覆盖建档、预约、接诊、检查、处方、住院、收费和复诊提醒。系统服务前台、兽医、护士、药房、宠物主人和院长，核心对象包括宠物档案、主人信息、疫苗史、病历、检查单、处方、住院床位和费用单。

1. 前台建立宠物档案、主人信息、疫苗史、过敏记录和体重基准，后端生成就诊编号。
2. 主人预约门诊、疫苗或手术时，系统按医生排班、科室容量和宠物风险标记返回可约时段。
3. 医生接诊录入主诉、体征、检查和诊断，处方必须校验体重剂量、禁忌和药品库存。
4. 检查项目、药品和耗材进入收费单，支付后药房发药或护士执行护理。
5. 住院宠物按床位记录喂食、用药、体温和病情变化，主人端查看授权摘要。
6. 复诊、疫苗和驱虫提醒按日期生成，前台跟进预约结果并沉淀客户生命周期。
7. 异常覆盖剂量错误、疫苗逾期、住院异常、费用争议、药品缺货和主人信息不符。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12664 宠物服务预约平台

请从宠物服务预约平台的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
宠物服务预约平台要把洗护、美容、寄养、接送、进度照片和评价复购做成服务闭环。系统服务宠物主人、门店、服务人员、司机和客服，核心对象包括宠物档案、疫苗证明、服务项目、预约单、接送任务、服务记录和费用单。

1. 商家配置洗护、美容、寄养、接送等服务项目、宠物限制、价格、耗材和可约时段。
2. 用户选择宠物档案预约服务，后端校验疫苗有效期、体型、攻击性标记和服务容量。
3. 门店确认后生成服务单，接送服务同步司机任务、预计到达时间和宠物交接照片。
4. 服务人员记录洗护步骤、造型照片、耗材、异常发现和完成时间，用户端实时查看进度。
5. 寄养服务按房间、天数、喂养要求和延住情况自动计费，异常状态推送主人确认。
6. 运营统计服务时长、复购、客诉、员工绩效和耗材成本。
7. 异常覆盖疫苗过期、宠物应激、接送延迟、寄养延住、服务投诉和费用争议。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12665 汽车维修保养系统

请从汽车维修保养系统的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
汽车维修保养系统要把车辆档案、预约、接车检测、报价、施工、质检、支付和下次提醒连成一条链路。系统服务车主、前台、技师、配件仓、店长和财务，核心对象包括车辆、工单、检测项、报价单、配件库存、施工记录和质保记录。

1. 车主建立车辆档案，录入车型、VIN、里程、保养周期和历史维修记录。
2. 门店配置保养套餐、工位、技师、配件库存和可约时段，后端计算可服务容量。
3. 车主预约到店后，前台接车检查外观、里程、故障描述和随车物品，生成维修保养工单。
4. 技师执行检测并提交报价，车主确认后系统扣减配件、记录施工步骤和工时。
5. 完工质检后车主验收支付，后端生成电子报告、质保范围和下次保养提醒。
6. 店长查看工位利用率、配件周转、返修率、技师产值和客户复购。
7. 异常覆盖报价争议、配件缺货、返修、里程异常、车主取消和工位冲突。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12666 汽车租赁平台

请从汽车租赁平台的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
汽车租赁平台要围绕车辆上架、驾照审核、押金支付、取还车验车、租期监控和违章结算展开。系统服务租客、门店、运营、风控、客服和财务，核心对象包括车辆、门店、租赁订单、押金、验车照片、轨迹、违章和结算单。

1. 运营维护车辆、门店、押金、租金、保险、限行和可租状态，后端记录车辆生命周期。
2. 用户实名认证并提交驾照，风控通过后才能下单、支付押金和选择取车门店。
3. 取车时用户或门店扫码验车，拍照记录油量、电量、里程和外观损伤，后端锁定取车基准。
4. 租期中系统记录定位、续租、救援、违章和超区域预警，异常同步客服和风控。
5. 还车后系统对比照片、里程、油电和违章冻结金额，生成结算单和押金释放计划。
6. 运营统计车辆利用率、损伤率、逾期率、违章率和门店调拨需求。
7. 异常覆盖驾照不合规、押金失败、车辆损伤、逾期未还、违章扣费和异地还车。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12667 二手车交易平台

请从二手车交易平台的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
二手车交易平台要把车源发布、检测报告、预约看车、订金锁车、合同过户和服务费结算做成可信交易链路。系统服务车商、买家、检测师、销售、客服和财务，核心对象包括车源、VIN、检测报告、意向单、订金、合同、过户资料和结算记录。

1. 车商发布车辆，录入 VIN、里程、配置、图片、价格和证件，后端校验重复车源和基础合规。
2. 检测师上传事故、水泡、火烧、维保、漆面和路试结果，报告审核后才能展示给买家。
3. 买家预约看车或试驾，系统记录意向、时间、销售跟进和车源锁定状态。
4. 买家支付订金后，后端锁定车辆，合同、尾款、金融方案和过户资料进入交易流程。
5. 过户完成后，系统更新车辆状态、释放或扣除订金，并按规则结算平台服务费。
6. 运营统计车源转化、价格调整、检测异常、退订原因和销售跟进效率。
7. 异常覆盖虚假车源、事故隐瞒、订金退款、过户失败、里程争议和重复售卖。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12668 车队管理系统

请从车队管理系统的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
车队管理系统要把车辆、司机、任务、轨迹、油耗、维修、违章和费用归集到同一套运营台账。系统服务调度、司机、维修人员、财务和车队经理，核心对象包括车辆档案、司机、任务单、定位轨迹、油卡、费用、维修和违章记录。

1. 调度维护车辆、司机、线路、车型、油耗标准和保养周期，后端生成车辆可派状态。
2. 业务方提交运输或用车任务，系统按车型、位置、司机工时、线路和优先级推荐车辆。
3. 司机接单后上报出车、到达、装卸、回场节点，定位轨迹进入任务记录并供调度监控。
4. 油卡、路桥费、停车费、维修和违章费用关联车辆、司机和任务，财务可复核。
5. 车辆保养到期或故障上报后退出可派状态，维修完成并验收后恢复派单能力。
6. 管理端统计里程、油耗、出勤、违章、维修成本和车辆利用率。
7. 异常覆盖司机超时、车辆故障、路线偏离、费用异常、违章未处理和保养逾期。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12669 加油站会员系统

请从加油站会员系统的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
加油站会员系统要围绕会员绑车、储值、优惠、加油流水、积分、发票和站点经营分析设计。系统服务会员、收银员、站长、总部运营和财务，核心对象包括会员、车辆、油品、枪号、交易流水、储值、积分、优惠券和发票。

1. 加油站配置油品、枪号、会员等级、优惠、积分和储值规则，后端记录每笔加油流水来源。
2. 会员注册并绑车后可领取优惠券或充值，系统校验券适用油品、站点、时段和会员等级。
3. 收银或自助加油完成后，交易流水回写会员积分、余额、优惠消耗和电子发票状态。
4. 站长处理油价调整、班次交接、油罐库存和枪号流水，库存差异进入复核。
5. 会员端查看加油记录、积分、储值、发票申请和异常扣款申诉。
6. 总部统计站点销量、毛利、会员复购、优惠成本和异常交易。
7. 异常覆盖油价切换、储值退款、枪号流水丢失、库存差异、优惠叠加和发票失败。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12670 便利店进销存系统

请从便利店进销存系统的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
便利店进销存系统要把商品资料、采购收货、收银销售、临期报损、盘点调拨和经营报表做成门店闭环。系统服务店长、采购、收银员、仓管、财务和总部运营，核心对象包括商品、条码、供应商、采购单、库存批次、销售流水、盘点单和报损单。

1. 店长维护商品、条码、供应商、售价、安全库存和效期规则，后端生成门店库存账。
2. 采购按补货建议下单，收货时扫码校验数量、批次、效期和供应商，差异进入收货异常。
3. 收银端销售时扣减库存、识别会员价和促销，后端写入小票流水和毛利数据。
4. 临期、报损、调拨和退货需要审批，系统保留库存调整原因和操作人。
5. 盘点任务按货架生成，盘盈盘亏进入复盘并影响损耗报表。
6. 经营看板统计销售、毛利、缺货、临期、损耗和供应商到货表现。
7. 异常覆盖条码重复、缺货、临期损耗、盘点差异、调拨丢失和会员价错误。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12671 生鲜称重收银系统

请从生鲜称重收银系统的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
生鲜称重收银系统要把 PLU、批次、称重、加工分割、会员促销、损耗和毛利核算连起来。系统服务收货员、称重收银员、店长、加工人员和财务，核心对象包括生鲜品类、PLU、批次库存、电子秤、收银流水、加工记录、报损和盘点。

1. 门店维护生鲜品类、称重 PLU、批次、进价、售价、会员价和损耗规则。
2. 收货时记录产地、批次、重量、等级和效期，后端生成可售库存和毛利基准。
3. 称重收银读取电子秤重量，系统按 PLU、会员价、促销和折扣权限计算金额。
4. 加工、分割和打包要记录原料批次、成品重量和损耗，成本回写到毛利报表。
5. 退货、报损和盘点差异按批次处理，不能只改总库存。
6. 店长看板查看毛利、损耗、滞销、称重异常和收银员差错。
7. 异常覆盖称重作弊、批次混淆、损耗异常、价格错配、退货争议和秤离线。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12672 批发市场交易系统

请从批发市场交易系统的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
批发市场交易系统要围绕商户摊位、买家交易、车辆过磅、市场收费、出场核验和价格行情设计。系统服务市场方、商户、买家、过磅员、门岗和财务，核心对象包括摊位、商户、交易单、车辆、过磅记录、结算单和保证金。

1. 市场方维护摊位、商户、品类、保证金、交易费率和车辆通行规则。
2. 买家下单或现场开单后，系统记录商品、重量、单价、车辆、司机和结算方式。
3. 过磅环节采集毛重、皮重、净重和磅单照片，后端锁定结算重量并防止重复过磅。
4. 结算时按货款、市场服务费、保证金扣减和欠费规则生成账单。
5. 车辆出场需要核验订单、缴费、车辆信息和门岗状态，异常进入人工处理。
6. 市场方统计交易额、品类价格、商户欠费、车辆周转和纠纷原因。
7. 异常覆盖过磅差异、商户欠费、车辆冒用、价格纠纷、退货冲账和保证金冻结。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12673 供应商协同交付平台

请从供应商协同交付平台的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
供应商协同交付平台要把采购计划、供应商承诺、发货预报、到货验收、质量异常和对账付款连成协同链路。系统服务采购方、供应商、仓库、质检和财务，核心对象包括采购订单、交付计划、ASN、装箱单、质检记录、到货差异、对账单和发票。

1. 采购方发布交付计划、物料清单、质量标准、到货窗口和扣罚规则。
2. 供应商确认产能、生产批次和发货时间，后端校验承诺日期与采购订单。
3. 发货前供应商上传装箱、质检、物流和预计到达信息，采购方提前安排收货资源。
4. 到货后仓库验收数量、批次和包装，质检录入质量结果，不合格进入让步接收或退货流程。
5. 对账按采购订单、到货、扣罚、退货和发票生成，双方确认后进入付款。
6. 协同看板展示准交率、合格率、异常关闭时长、供应商评分和未到货风险。
7. 异常覆盖延期交付、质量不合格、少发多发、发票不符、对账差异和供应商越权。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12674 订单履约中心 OMS

请从订单履约中心 OMS的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
订单履约中心 OMS 要统一接收多渠道订单，完成库存分配、拆单、仓库履约、物流回传、拦截和售后协同。系统服务渠道运营、仓库、客服、财务和管理者，核心对象包括渠道订单、履约单、库存占用、拆单结果、包裹、物流节点和售后关联。

1. OMS 接收来自商城、门店和第三方渠道的订单，后端做幂等校验并统一生成履约单。
2. 系统按库存地、物流时效、商品属性、客户地址和仓库能力选择仓库与承运商。
3. 拆单结果生成包裹和拣货任务，仓库回传拣货、复核、打包、出库和取消节点。
4. 缺货、改地址、取消和物流拦截由 OMS 统一决策，避免渠道和仓库各自改状态。
5. 售后、退款、补发必须关联原履约单，库存、财务和客服记录同步处理。
6. 运营监控履约时效、拆单率、缺货率、承运商表现和渠道异常。
7. 异常覆盖渠道重复推单、拆单失败、库存不足、仓库拒单、物流拦截和状态回传丢失。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12675 售后退换货系统

请从售后退换货系统的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
售后退换货系统要覆盖退货、换货、维修、仅退款、入仓质检、退款补发和责任分析。系统服务用户、客服、仓库质检、商家、财务和运营，核心对象包括售后单、订单明细、证据材料、退货物流、质检记录、退款流水和补发单。

1. 用户按订单明细发起退货、换货、维修或仅退款，系统校验签收时间、商品类目和可售后规则。
2. 客服审核证据材料、责任原因和诉求，自动通过或人工审核都要记录依据和处理方案。
3. 退货入仓后质检确认商品、数量、状态和责任，后端决定退款、换货、维修、拒收或折价。
4. 退款、补发和维修完成后同步订单、库存、财务、商家结算和客服记录。
5. 商家可查看自己商品的售后原因和责任结果，但不能越权修改平台裁定。
6. 售后报表统计原因、处理时长、责任方、退款金额、二次投诉和商品质量问题。
7. 异常覆盖超期申请、恶意退货、退回不符、退款失败、换货缺货和责任争议。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12676 客户成功管理系统

请从客户成功管理系统的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
客户成功管理系统要把客户档案、合同订阅、产品用量、健康分、风险跟进和续费机会串起来。系统服务 CSM、销售、客服、实施、客户联系人和管理层，核心对象包括客户、合同、订阅、联系人、用量、工单、跟进计划和续费机会。

1. 客户成功经理维护客户、合同、订阅、联系人、业务目标和成功计划。
2. 系统接入产品用量、工单、培训、会议和满意度数据，计算客户健康分和风险标签。
3. CSM 制定跟进计划，记录会议纪要、风险、承诺事项、负责人和下一步动作。
4. 客户风险触发升级时，经理端看到流失原因、影响金额、挽回策略和协同人员。
5. 续费机会按合同到期、用量、健康分和满意度生成，销售与 CSM 协同推进。
6. 管理层查看客户健康、续费预测、风险关闭率、服务成本和团队负载。
7. 异常覆盖联系人变更、客户停用、续费延期、承诺未完成、数据缺失和权限泄露。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12677 SaaS 租户管理系统

请从SaaS 租户管理系统的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
SaaS 租户管理系统要把租户开通、套餐、成员、权限、资源额度、计费和停用隔离设计清楚。系统服务平台管理员、租户管理员、财务、运维和客服，核心对象包括租户、套餐、组织、成员、角色、资源用量、账单和审计日志。

1. 平台创建租户、套餐、管理员、域名、功能开关和资源额度，后端生成隔离空间和默认权限。
2. 租户管理员配置组织、角色、成员和业务参数，所有数据查询都必须带租户边界。
3. 计费系统按席位、用量、插件、超额资源和账期生成账单，欠费状态影响功能可用范围。
4. 租户升级、降级、暂停和注销要处理功能开关、数据保留、访问限制和恢复窗口。
5. 运维端查看租户资源、异常登录、API 调用、慢查询和审计日志。
6. 运营统计套餐转化、流失、资源成本、付费留存和欠费风险。
7. 异常覆盖租户串数据、额度超限、欠费停用、域名冲突、套餐降级和管理员误删。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12678 应用市场插件平台

请从应用市场插件平台的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
应用市场插件平台要覆盖开发者提交、权限审核、插件发布、租户安装、版本升级、计费分成和安全下架。系统服务开发者、平台审核员、租户管理员、运维和财务，核心对象包括插件、版本、权限声明、安装记录、依赖、订阅和收益。

1. 开发者提交插件包、权限声明、版本说明、截图、收费方式和兼容版本，平台进入审核队列。
2. 审核员检查安全权限、依赖冲突、数据访问范围和 UI 规范，通过后插件发布到应用市场。
3. 租户安装插件时，后端校验套餐、依赖、权限授权和数据范围，生成安装记录和配置项。
4. 插件升级支持灰度、回滚、版本锁定和变更提示，失败时恢复旧版本配置。
5. 计费按订阅、席位或用量生成账单，开发者端查看收益、安装量和评价。
6. 平台监控插件崩溃、调用量、卸载原因和安全告警，必要时下架并通知租户。
7. 异常覆盖恶意权限、依赖冲突、升级失败、计费争议、插件下架和租户数据越权。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12679 API 开放平台

请从API 开放平台的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
API 开放平台要把开发者入驻、应用密钥、权限授权、签名校验、限流、回调和审计日志做成可运营平台。系统服务开发者、平台管理员、审核人员、运维和业务系统，核心对象包括应用、密钥、权限范围、接口、调用日志、回调事件和限流策略。

1. 平台维护接口目录、权限范围、回调地址、限流策略和应用审核规则。
2. 开发者创建应用并申请接口权限，审核通过后生成 access key、secret 和可调用资源。
3. 每次 API 调用经过签名、时间戳、IP 白名单、额度和权限校验，结果写入调用日志。
4. 回调事件支持投递、重试、死信、手工补发和签名验证，开发者端可查看每次投递记录。
5. 管理员可按应用调整限流、封禁、密钥轮换和接口版本，变更会影响调用侧提示。
6. 运营统计接口调用、错误率、慢请求、开发者活跃、付费额度和封禁原因。
7. 异常覆盖签名失败、密钥泄露、回调失败、额度耗尽、版本废弃和恶意刷接口。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12680 开发者文档站

请从开发者文档站的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
开发者文档站要把产品版本、接口说明、示例代码、变更日志、反馈工单和搜索分析结合起来。系统服务文档作者、编辑、开发者、产品和运营，核心对象包括文档页、版本、API 示例、代码片段、变更日志、反馈和搜索记录。

1. 文档作者维护产品、版本、目录、接口示例、错误码和变更日志，草稿与发布版本分离。
2. 编辑审核术语、代码示例、链接、版本适配和敏感信息，通过后发布到文档站。
3. 开发者按语言、版本、关键词和场景检索文档，示例代码可复制并关联接口调试。
4. API 变更时系统标记废弃字段、替代方案、迁移期限和影响版本，旧版本仍可访问。
5. 反馈入口收集错误、缺失、看不懂和示例失败，工单分派给对应文档负责人。
6. 运营统计搜索无结果、热门页面、反馈关闭率、版本访问和示例复制率。
7. 异常覆盖示例错误、版本错配、链接失效、权限文档泄露、反馈积压和发布回滚。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12681 命令行工具包

请从命令行工具包的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
命令行工具包要围绕初始化、登录、配置、脚手架、构建、部署、诊断和插件扩展设计，不要只做帮助文案。系统服务开发者、运维和平台，核心对象包括命令、参数、配置、令牌、模板、执行日志和插件。

1. CLI 初始化项目时读取模板、语言、包管理器和目标目录，生成过程写入本地日志和可回放配置。
2. 命令执行前校验参数、环境变量、依赖版本和目录权限，错误信息要给出可执行修复建议。
3. 登录和令牌管理支持过期刷新、权限范围、本机安全存储和登出清理。
4. 脚手架、构建、部署和诊断命令输出结构化日志，便于终端、GUI 或 CI 系统读取。
5. 插件命令按命名空间加载，命令冲突、版本不兼容和危险操作需要阻断或确认。
6. 遥测只收集用户允许的数据，管理端可查看命令失败率、常见环境问题和模板使用情况。
7. 异常覆盖参数缺失、依赖不兼容、令牌过期、模板下载失败、插件冲突和权限不足。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12682 文件整理 Web 管理工具

请从文件整理 Web 管理工具的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
文件整理 Web 管理工具要把扫描、分类、重复识别、预览、执行、撤销和规则模板做成安全工具。系统服务个人用户和团队管理员，核心对象包括目录、文件、规则、重复哈希、执行计划、操作记录和撤销记录。

1. 用户选择扫描目录和整理规则，工具先生成预览清单，不直接移动或删除文件。
2. 系统按扩展名、时间、大小、重复哈希、命名模式和文件内容识别文件类型。
3. 用户确认后执行移动、重命名、归档或删除，所有动作写入可撤销记录和失败明细。
4. 重复文件处理展示保留建议、路径、修改时间和大小，避免误删重要版本。
5. 规则模板可保存、分享并定时运行，运行结果进入历史和异常清单。
6. 统计面板展示节省空间、重复文件、失败操作、目录增长趋势和常用规则。
7. 异常覆盖误删恢复、权限不足、文件占用、同名冲突、外置盘断开和规则误匹配。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12683 桌面截图标注工具

请从桌面截图标注工具的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
桌面截图标注工具要把截图、标注图层、敏感遮挡、历史库、导出和快捷键管理做成完整体验。系统服务个人用户、产品、测试和设计团队，核心对象包括截图原图、编辑层、标注对象、标签、历史记录和导出文件。

1. 用户触发截图后进入标注画布，系统保存原图、编辑层、来源窗口和导出版本。
2. 标注工具支持箭头、框选、文字、马赛克、序号、颜色和样式，所有对象可二次编辑。
3. 保存时记录文件名、来源应用、时间、标签和备注，便于后续检索。
4. 分享或导出可选择格式、分辨率、是否隐藏敏感区域和是否复制到剪贴板。
5. 历史库支持按应用、标签、日期、文字备注和颜色标记查找截图。
6. 设置页管理快捷键、默认存储路径、自动清理、云同步和隐私保护。
7. 异常覆盖快捷键冲突、保存失败、隐私未遮挡、画布崩溃、文件重名和剪贴板权限。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12684 数据库管理 Web 平台

请从数据库管理 Web 平台的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
数据库管理 Web 平台要把连接管理、结构浏览、SQL 执行、结果编辑、事务控制和审计日志设计清楚。系统服务开发者、数据分析师和 DBA，核心对象包括连接、数据库、表结构、查询脚本、结果集、事务和操作历史。

1. 用户创建数据库连接，系统安全存储连接串、SSH 隧道、只读模式和环境标签。
2. 连接成功后加载库表、字段、索引、视图和行数，前端提供结构树和查询编辑器。
3. SQL 执行区分只读、变更和危险语句，事务操作必须有确认、提交、回滚和历史记录。
4. 结果表支持分页、筛选、导出、单行详情和复制，慢查询记录执行计划。
5. 数据编辑需要权限校验、差异预览、主键识别和审计日志，防止误改生产数据。
6. 连接监控展示耗时、错误、历史查询、收藏脚本和常用数据库状态。
7. 异常覆盖连接失败、权限不足、危险 SQL、事务未提交、导出过大和隧道断开。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12685 桌面财务记账工具

请从桌面财务记账工具的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
桌面财务记账工具要把账户、分类、预算、流水、周期账单、凭证和报表穿透做成个人或小团队可用系统。系统服务记账用户和家庭成员，核心对象包括账户、币种、分类、预算、流水、商户、附件和周期任务。

1. 用户建立现金、银行卡、信用卡、电子钱包、币种、分类和初始余额。
2. 记账时选择收入、支出、转账、报销或借贷，后端生成流水并更新账户余额。
3. 周期账单、房租、贷款分期和订阅自动生成待确认记录，用户确认后入账。
4. 预算按分类、账户和周期预警，超支原因可关联具体流水和商户。
5. 报表按账户、分类、商户、成员和时间分析，支持穿透到原始凭证和附件。
6. 数据导入导出要做重复识别、字段映射、汇率处理和隐私保护。
7. 异常覆盖重复流水、余额不平、分类误判、预算超支、导入失败和汇率变动。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12686 浏览器插件价格追踪器

请从浏览器插件价格追踪器的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
浏览器插件价格追踪器要把商品解析、价格历史、优惠识别、降价提醒和跨平台对比做成可信工具。系统服务网购用户和运营分析人员，核心对象包括关注商品、URL、规格、价格快照、优惠、提醒规则和历史曲线。

1. 用户添加关注商品 URL，插件解析标题、规格、图片、价格、商家和平台来源。
2. 后台定时抓取价格，识别促销、券后价、会员价、运费和历史最低价。
3. 价格达到阈值或低于历史均值时推送浏览器通知、邮件或站内提醒。
4. 商品详情展示价格曲线、平台对比、优惠有效期和规格变更提示。
5. 用户可管理分组、屏蔽商家、合并同款和清理失效链接。
6. 统计页展示降价成功、提醒点击、价格异常和平台抓取成功率。
7. 异常覆盖反爬拦截、价格解析错误、商品下架、规格变化、通知失败和链接失效。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12687 浏览器插件网页剪藏工具

请从浏览器插件网页剪藏工具的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
浏览器插件网页剪藏工具要把网页内容抓取、正文清洗、快照保存、批注、标签和多端同步做成知识管理工具。系统服务研究者、产品、运营和个人用户，核心对象包括网页快照、正文、图片、选区、高亮、批注、标签和笔记本。

1. 用户剪藏网页时，插件提取标题、正文、图片、来源 URL、选区内容和发布时间。
2. 后端保存原始快照、清洗正文、标签、引用信息和离线资源，支持断网查看。
3. 用户可高亮、批注、整理到笔记本，并保留网页来源和引用格式。
4. 重复剪藏时系统提示合并、覆盖或创建新版本，避免同一资料散落。
5. 全文搜索按标签、来源、日期、正文和批注命中定位到笔记。
6. 同步任务记录冲突、失败、多端版本和恢复入口。
7. 异常覆盖网页解析失败、图片失效、重复剪藏、同步冲突、版权敏感和离线保存失败。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12688 自动化爬虫采集工具

请从自动化爬虫采集工具的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
自动化爬虫采集工具要把目标配置、字段解析、登录态、调度、去重、质量校验和导出做成可控平台。系统服务数据采集团队、运营和管理员，核心对象包括采集任务、目标站点、字段规则、代理、请求日志、数据集和质量报告。

1. 用户配置采集目标、字段、分页、登录态、频率和去重键，系统先运行小样本预检。
2. 任务执行时记录请求、解析、去重、入库数量和失败 URL，支持断点续跑和失败重试。
3. 字段解析支持规则版本，目标站变化后旧数据保留来源、规则号和采集时间。
4. 代理、限速、重试和并发策略由后端统一调度，避免目标站点被打爆。
5. 采集结果进入数据集，支持字段校验、缺失率分析、导出和质量评分。
6. 看板展示任务成功率、错误类型、字段缺失、采集成本和站点风险。
7. 异常覆盖反爬封禁、字段变更、登录过期、重复数据、频率超限和代理失败。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12689 自动化报表生成工具

请从自动化报表生成工具的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
自动化报表生成工具要把数据源、指标口径、模板、调度、审批、分发和异常拦截设计成报表生产线。系统服务业务人员、数据分析师、审批人和管理者，核心对象包括数据源、指标、模板、生成任务、审批记录、附件和发送日志。

1. 用户配置数据源、报表模板、指标口径、过滤条件和发送计划。
2. 系统按计划抽取数据，记录源表版本、查询条件、计算过程和指标快照。
3. 报表生成后支持预览、审批、导出 PDF/Excel 和定时分发。
4. 指标异常、数据缺失或口径变更时，系统标记差异原因并阻止自动发送错误报表。
5. 接收人权限决定可见明细和附件范围，外发记录进入审计日志。
6. 管理端统计报表成功率、生成耗时、订阅人数、失败原因和人工修改记录。
7. 异常覆盖数据源断开、指标口径错误、附件过大、发送失败、权限泄露和模板损坏。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12690 RPA 流程机器人平台

请从RPA 流程机器人平台的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
RPA 流程机器人平台要把流程编排、机器人账号、运行日志、人工审批、断点重跑和效果统计做成企业自动化平台。系统服务流程设计者、执行机器人、审批人、运维和业务负责人，核心对象包括流程版本、步骤、凭证、运行实例、截图、异常和审批节点。

1. 流程设计者录制或编排网页、桌面、文件和接口操作步骤，系统保存流程版本和参数。
2. 运行前校验机器人账号、目标系统、文件路径、执行窗口和敏感权限。
3. 机器人执行时记录每一步截图、输入、输出、耗时和异常分支。
4. 人工审批节点暂停流程并推送待办，审批结果决定后续路径和是否继续执行。
5. 失败任务可从断点重跑，涉及付款、删除或提交的敏感操作需要二次确认和审计。
6. 运营看板统计节省工时、成功率、失败步骤、机器人占用和流程收益。
7. 异常覆盖元素定位失败、账号锁定、验证码、文件缺失、审批超时和重复执行。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12691 自动化测试平台

请从自动化测试平台的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
自动化测试平台要把项目、环境、用例、数据集、执行计划、缺陷草稿和验收报告连成测试闭环。系统服务测试负责人、测试人员、开发、产品和发布经理，核心对象包括项目、环境、用例、测试数据、执行任务、截图日志、缺陷和报告。

1. 测试负责人维护项目、环境、版本、用例集、数据集和执行计划。
2. 测试人员编写接口、UI 和流程用例，后端管理依赖、断言、优先级和测试数据隔离。
3. 执行任务拉起浏览器或接口请求，记录截图、日志、请求响应、耗时和环境信息。
4. 失败用例自动归类为断言失败、环境问题、脚本问题或数据污染，并生成缺陷草稿。
5. 回归计划按版本变更和影响范围选择用例，结果与发布验收关联。
6. 看板统计通过率、flaky 用例、缺陷分布、环境稳定性和版本质量。
7. 异常覆盖环境不可用、用例不稳定、数据污染、断言误报、浏览器崩溃和报告丢失。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12692 UI 组件库文档站

请从UI 组件库文档站的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
UI 组件库文档站要把组件示例、属性表、设计 token、版本迁移、在线调试和反馈工单组合起来。系统服务前端开发、设计师、组件维护者和产品团队，核心对象包括组件、示例、属性、主题、版本、变更日志和反馈。

1. 维护者编写组件文档、示例、属性表、设计约束和可访问性说明，系统按版本发布。
2. 组件示例支持在线切换属性、主题、尺寸和状态，代码片段与实际渲染保持一致。
3. 组件 API 变更时记录废弃字段、迁移指南、影响组件和最低兼容版本。
4. 设计 token、主题、图标和暗色模式统一管理，前端示例实时读取配置。
5. 用户反馈文档错误、示例缺失或组件缺陷后，进入维护者处理队列。
6. 统计热门组件、搜索无结果、版本迁移、示例运行错误和反馈关闭率。
7. 异常覆盖示例失效、属性表错误、主题冲突、版本不兼容、文档遗漏和构建失败。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12693 表单校验引擎

请从表单校验引擎的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
表单校验引擎要把字段定义、规则版本、前后端一致校验、联动条件、错误提示和统计分析做成规则服务。系统服务产品、前端、后端、运营和表单填写用户，核心对象包括表单、字段、规则、版本、提交记录、错误记录和灰度配置。

1. 产品配置字段类型、必填、格式、联动条件、错误提示和服务端校验规则，后端生成规则版本。
2. 前端表单渲染时从后端读取规则，不在页面硬编码必填、格式或联动逻辑。
3. 用户输入触发本地提示和服务端复核，复杂规则、唯一性和跨字段校验以服务端结果为准。
4. 规则变更支持灰度、回滚和历史版本，已提交表单按提交时规则追溯。
5. 校验失败记录字段、原因、用户修正路径和放弃节点，帮助产品优化表单。
6. 管理端统计错误率最高字段、放弃率、规则命中和批量导入失败原因。
7. 异常覆盖规则冲突、历史版本缺失、联动循环、错误提示不清、绕过校验和批量导入失败。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12694 工作流引擎平台

请从工作流引擎平台的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
工作流引擎平台要把流程建模、表单、节点、权限、分支、加签、超时和实例追踪做成可复用流程底座。系统服务流程管理员、发起人、审批人、业务负责人和运维，核心对象包括流程定义、版本、节点、表单、实例、待办、审批记录和超时规则。

1. 管理员建模流程、节点、角色、表单、超时、分支条件和通知规则，发布后形成流程版本。
2. 发起人提交申请后，后端生成流程实例、业务编号和第一个待办。
3. 审批人处理同意、驳回、转交、加签、撤回和抄送，系统记录意见、附件和状态轨迹。
4. 条件分支按金额、部门、风险、地区或字段值自动流转，前端只展示当前节点允许动作。
5. 超时任务触发提醒、升级、自动转派或自动流转，所有结果可追溯到流程版本。
6. 流程报表统计办理时长、驳回率、瓶颈节点、人员负载和异常实例。
7. 异常覆盖流程死循环、越权审批、表单变更、超时未处理、版本切换和重复提交。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

## wv2-12695 规则引擎决策平台

请从规则引擎决策平台的真实岗位和业务单据出发，先把发起、处理、复核、异常和报表链路梳理清楚，再组织前后端代码，避免只生成静态管理页。
规则引擎决策平台要把业务规则、事实输入、决策结果、解释路径、灰度发布和效果复盘做成可审计能力。系统服务业务配置人员、开发者、审核人、风控和运营，核心对象包括规则集、条件、动作、事实模型、测试样本、决策日志和版本。

1. 业务人员维护规则集、条件、动作、优先级、生效范围和灰度比例，系统保存版本。
2. 调用方提交事实数据后，规则引擎返回命中规则、决策结果、解释路径和建议动作。
3. 规则测试台支持样本回放、边界值验证、冲突检测和命中率预估。
4. 发布规则需要审批、灰度和监控，异常命中率或业务投诉触发回滚建议。
5. 决策结果写入业务单据，后续人工改判必须说明原因并保留原决策。
6. 看板统计规则命中、拒绝率、人工改判、效果对比和调用耗时。
7. 异常覆盖规则冲突、条件缺失、灰度误配、决策不可解释、版本回滚和调用超时。
   落地要求：统一实现为浏览器 Web 全栈业务系统，不要做独立 App、移动端安装包、小程序壳、桌面客户端或纯前端静态页面；必须包含 Web 前端、后端 API、SQLite 数据库和必要的管理/运营视图，所有核心动作都要落库并可复查。
   组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
   端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 may-8744，端口必须包含 8744，可使用 87441、87442 或 18744、28744 这类合法前后缀组合；五位项目号例如 may-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 may-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
   验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
   前后端启动方式统一使用后台启动，关闭终端不影响程序运行，只有手动关闭对应端口进程才算完成关闭。
   不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
   在执行阶段，涉及项目内删除操作时无需再征求我的同意。
   按照需求功能的顺序，每一轮都优先考虑搭建核心功能，在逐步的考虑其他功能

