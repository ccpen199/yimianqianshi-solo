# 项目业务复杂代码生成需求 251-300

## 251 银行开户与客户尽调系统

生成银行开户与客户尽调系统时，请把前端操作、后端校验、数据库状态和报表来源对齐，避免页面能点但业务事实不可追溯。
银行开户尽调要把开户申请、身份核验、受益所有人、KYC/AML筛查、风险评级和合规复核放在严格审计链路里。
核心数据对象包括客户档案、证件影像、受益所有人、材料清单、筛查结果、风险评级、审核意见；关键规则要落到身份核验、名单筛查、风险评级、材料完整性校验，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 客户提交开户申请和证件，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 客户经理补充企业与受益人信息，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 系统完成身份核验和黑名单筛查，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 合规人员复核高风险命中，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 账户运营开立账户并归档材料，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 后续定期尽调触发更新，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
7. 异常处理必须覆盖证件过期、受益人不明、名单命中、材料缺失、重复客户、尽调过期，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
8. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 252 银行贷款贷后管理系统

请按银行贷款贷后管理系统的一线业务来组织实现，先把岗位、对象、状态和异常讲清楚，再让页面、接口和数据表围绕这些事实展开。
贷后管理要围绕放款后的还款计划、逾期监测、抵押物、贷后检查、催收展期和结清闭环。
核心数据对象包括贷款合同、还款计划、还款流水、逾期记录、抵押物、检查报告、催收记录；关键规则要落到还款计划生成、逾期分级、风险预警、展期结清核算，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 放款后生成还款计划，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 系统每日匹配还款流水，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 逾期自动分级和提醒，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 客户经理执行贷后检查，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 风控评估抵押物和经营变化，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 催收或展期方案审批，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 结清后释放抵押并归档，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖提前还款、部分还款、抵押物贬值、逾期失联、展期争议、罚息重算，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 253 证券交易订单系统

证券交易订单系统不能做成通用后台换皮，首版要直接服务真实使用者，把核心流转、规则计算、异常分支和统计口径放进同一套实现。
证券订单系统要保证账户、资金、持仓、委托、成交和撤单的状态一致，任何交易动作都要可回放。
核心数据对象包括账户、资金余额、持仓、委托单、成交回报、撤单请求、风控记录；关键规则要落到资金持仓校验、委托撮合接入、成交回报处理、撤单冻结释放，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 投资者提交买卖委托，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 系统校验资金持仓和风险限制，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 委托进入交易通道，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 接收成交回报更新持仓资金，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 未成交部分可撤单释放冻结，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 日终对账生成差异清单，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 用户查看流水和持仓，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖重复下单、资金冻结异常、部分成交、撤单失败、行情延迟、日终不平，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 254 基金申购赎回平台

落到基金申购赎回平台这个项目，复杂度应该来自业务本身：谁发起、谁处理、系统如何校验、结果如何归档，都要能在代码里找到依据。
基金交易平台要覆盖风险测评、产品浏览、申购扣款、份额确认、收益展示、赎回到账和定投管理。
核心数据对象包括基金产品、净值、风险测评、申购单、赎回单、份额、定投计划；关键规则要落到风险匹配、交易日历、份额确认、收益计算，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 用户完成风险测评，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 浏览匹配风险等级基金，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 提交申购并扣款，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 按交易日确认份额，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 展示持仓收益和净值走势，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 用户发起赎回，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 到账后更新资金流水，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖非交易日、风险不匹配、扣款失败、份额确认延迟、巨额赎回、收益口径争议，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 255 支付清算对账系统

生成支付清算对账系统时，请把前端操作、后端校验、数据库状态和报表来源对齐，避免页面能点但业务事实不可追溯。
支付清算对账要把内部订单、支付流水、渠道账单、银行流水、自动匹配、差错处理和调账做成财务可验的链路。
核心数据对象包括内部订单、支付流水、渠道账单、银行流水、匹配规则、差错单、调账凭证；关键规则要落到流水匹配、手续费核算、差错识别、调账审批，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 导入内部订单和支付流水，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 拉取渠道账单和银行流水，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 系统按金额时间订单号匹配，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 未匹配项生成差错单，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 财务确认手续费和汇差，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 调账后重新对账，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 出具日清报表，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖重复回调、手续费差异、银行延迟入账、退款跨日、渠道漏单、调账凭证缺失，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 256 税务申报管理系统

请按税务申报管理系统的一线业务来组织实现，先把岗位、对象、状态和异常讲清楚，再让页面、接口和数据表围绕这些事实展开。
税务申报要围绕税种、发票、进项销项、申报表、缴款和更正申报，保证数据来源和口径可追溯。
核心数据对象包括纳税主体、税种、发票、申报表、抵扣明细、缴款书、更正记录；关键规则要落到税额计算、发票抵扣、申报校验、逾期提醒，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 财务维护纳税主体和税种，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 导入销项进项发票，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 系统生成申报表和抵扣明细，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 财务复核后提交申报，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 缴款结果回填，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 更正申报保留原版本，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 管理端查看税负趋势，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖发票异常、税率变化、申报逾期、抵扣不足、缴款失败、更正追溯，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 257 审计底稿管理系统

审计底稿管理系统不能做成通用后台换皮，首版要直接服务真实使用者，把核心流转、规则计算、异常分支和统计口径放进同一套实现。
审计底稿要覆盖项目立项、底稿编制、证据附件、复核意见、问题整改和归档锁定。
核心数据对象包括审计项目、底稿、证据附件、复核意见、整改项、归档版本；关键规则要落到底稿模板、证据完整性、复核流、归档锁定，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 项目经理创建审计项目，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 审计员按科目编制底稿，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 上传证据并关联抽样记录，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 复核人提出意见，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 审计员修改并回复，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 问题形成整改跟踪，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 归档后只允许补录和重开，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖证据缺失、复核退回、版本覆盖、整改逾期、权限越界、归档后修改，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 258 发票查验与风险识别系统

落到发票查验与风险识别系统这个项目，复杂度应该来自业务本身：谁发起、谁处理、系统如何校验、结果如何归档，都要能在代码里找到依据。
发票查验系统要把发票采集、OCR识别、税局查验、重复检测、供应商风险和报销归档串起来。
核心数据对象包括发票图片、OCR结果、查验记录、供应商、风险标签、报销单、重复库；关键规则要落到OCR识别、发票真伪查验、重复检测、风险评分，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 员工上传发票图片，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. OCR提取号码代码金额，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 系统调用或模拟查验真伪，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 与历史库做重复检测，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 结合供应商风险打标签，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 进入报销或归档，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 审计查看异常清单，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖图片模糊、重复报销、发票作废、供应商异常、税号不符、查验接口失败，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 259 出版选题与稿件管理系统

生成出版选题与稿件管理系统时，请把前端操作、后端校验、数据库状态和报表来源对齐，避免页面能点但业务事实不可追溯。
出版系统要围绕选题申报、立项审批、作者签约、稿件提交、审稿编辑和出版排期展开。
核心数据对象包括选题、作者、合同、稿件版本、审稿意见、编辑加工记录、排期；关键规则要落到选题审批、版本对比、审稿分派、合同节点控制，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 编辑提交选题和市场分析，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 主编审批立项，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 作者签署合同和交稿计划，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 作者上传稿件版本，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 审稿人给出修改意见，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 编辑加工定稿，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 出版管理排期和归档，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖选题撞车、作者延期、审稿意见泄露、版本混乱、合同未签、排期变更，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 260 新闻采编发布系统

请按新闻采编发布系统的一线业务来组织实现，先把岗位、对象、状态和异常讲清楚，再让页面、接口和数据表围绕这些事实展开。
新闻采编发布要从线索、采访、写稿、编辑审核、主编终审、定时发布和多渠道分发展开。
核心数据对象包括线索、采访任务、稿件、审核流、栏目、发布计划、阅读数据；关键规则要落到稿件审核、定时发布、多渠道分发、阅读复盘，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 记者提交线索并领取采访任务，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 写稿上传素材和来源，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 编辑审核事实和标题，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 主编终审敏感内容，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 系统定时或即时发布，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 分发到App/网站/社媒，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 运营复盘阅读和互动，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖突发撤稿、来源缺失、敏感词命中、定时失败、栏目错发、版权投诉，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 261 广告投放预算管理系统

广告投放预算管理系统不能做成通用后台换皮，首版要直接服务真实使用者，把核心流转、规则计算、异常分支和统计口径放进同一套实现。
广告预算管理要围绕项目、渠道、预算、素材、花费、转化和ROI复盘，支持投放过程中的预算控制。
核心数据对象包括投放项目、渠道、预算包、素材、花费数据、转化数据、预警记录；关键规则要落到预算分配、花费同步、超支预警、ROI计算，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 品牌方创建投放项目，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 财务分配总预算和渠道预算，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 投放人员上传素材并启动投放，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 系统同步渠道花费和转化，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 超支或低ROI触发调整建议，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 财务复核消耗，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 管理层查看投放报表，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖渠道币种差异、素材驳回、花费延迟、预算超支、转化归因争议、发票不匹配，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 262 广告素材管理平台

落到广告素材管理平台这个项目，复杂度应该来自业务本身：谁发起、谁处理、系统如何校验、结果如何归档，都要能在代码里找到依据。
广告素材管理要覆盖素材上传、标签、版权授权、审核、投放使用、效果记录和过期下架。
核心数据对象包括素材文件、标签、版权授权、审核记录、投放记录、效果数据、下架任务；关键规则要落到素材去重、版权到期预警、审核发布、使用追踪，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 设计师上传图片视频和源文件，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 素材管理员添加标签和尺寸，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 法务维护版权授权期限，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 审核通过后投放人员领取使用，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 投放效果回写素材维度，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 授权过期自动下架，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 后台分析素材复用率，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖素材重复、版权过期、尺寸不符、审核驳回、外链失效、误用未授权素材，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 263 游戏运营活动后台

生成游戏运营活动后台时，请把前端操作、后端校验、数据库状态和报表来源对齐，避免页面能点但业务事实不可追溯。
游戏运营活动后台要支持活动配置、奖励规则、目标人群、上线灰度、数据监控和异常补偿。
核心数据对象包括活动、奖励、玩家分群、任务条件、发奖记录、灰度计划、数据看板；关键规则要落到玩家分群、奖励发放、灰度开关、异常补偿，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 运营创建活动和任务条件，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 配置奖励和发放上限，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 选择玩家分群和灰度范围，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 活动上线后实时监控参与，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 玩家完成任务自动发奖，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 异常补发或回收，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 复盘留存和付费转化，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖刷奖励、发奖延迟、配置误发、灰度扩大、活动冲突、玩家投诉，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 264 游戏玩家客服与封禁系统

请按游戏玩家客服与封禁系统的一线业务来组织实现，先把岗位、对象、状态和异常讲清楚，再让页面、接口和数据表围绕这些事实展开。
玩家客服封禁系统要把工单、聊天证据、违规检测、处罚、申诉和解封流程做成可审计链路。
核心数据对象包括玩家账号、客服工单、聊天记录、违规证据、封禁记录、申诉单；关键规则要落到违规规则、处罚阶梯、证据留存、申诉复核，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 玩家提交问题或举报，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 客服查看账号和日志，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 系统识别违规行为，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 按处罚阶梯封禁或禁言，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 玩家发起申诉，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 复核员查看证据决定维持或解封，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 管理端统计投诉类型，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖误封、证据缺失、外挂批量账号、辱骂刷屏、申诉超时、处罚不一致，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 265 游戏内商城系统

游戏内商城系统不能做成通用后台换皮，首版要直接服务真实使用者，把核心流转、规则计算、异常分支和统计口径放进同一套实现。
游戏商城要围绕商品配置、限购、定价、支付、发货、退款和活动折扣，确保虚拟资产账务一致。
核心数据对象包括虚拟商品、价格、限购规则、订单、支付流水、发货记录、退款单；关键规则要落到限购校验、支付回调、虚拟发货、折扣叠加，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 运营配置礼包和上架时间，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 玩家浏览商城并下单，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 系统校验等级限购和活动价，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 支付成功后发放道具，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 道具到账写入资产流水，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 退款或补发进入客服流程，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 财务查看收入报表，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖重复发货、支付漏单、限购绕过、礼包配置错误、退款争议、资产回滚，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 266 社区内容治理系统

落到社区内容治理系统这个项目，复杂度应该来自业务本身：谁发起、谁处理、系统如何校验、结果如何归档，都要能在代码里找到依据。
社区治理要覆盖内容发布、机器识别、人工审核、举报、处罚和申诉，保护社区秩序。
核心数据对象包括帖子、评论、用户、审核结果、举报、处罚记录、申诉单；关键规则要落到敏感识别、举报优先级、处罚阶梯、申诉复核，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 用户发布内容，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 系统机审识别风险等级，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 高风险进入人工审核，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 普通内容开放举报入口，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 审核员处罚或恢复内容，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 用户可申诉，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 管理端复盘违规趋势，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖误判、恶意举报、敏感内容扩散、处罚过重、申诉积压、审核员越权，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 267 社区积分与等级系统

生成社区积分与等级系统时，请把前端操作、后端校验、数据库状态和报表来源对齐，避免页面能点但业务事实不可追溯。
积分等级系统要围绕行为计分、等级权益、任务、勋章、风控扣分和成长看板展开。
核心数据对象包括用户、积分流水、等级、任务、勋章、权益、风控记录；关键规则要落到行为计分、等级升级、权益发放、作弊识别，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 运营配置积分规则和等级权益，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 用户发帖互动获得积分，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 系统记录积分流水并判断升级，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 完成任务发放勋章，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 违规行为扣分或冻结权益，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 用户查看成长路径，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 运营分析活跃贡献，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖刷积分、规则变更、权益重复发放、扣分争议、等级降级、历史积分迁移，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 268 知识产权商标管理系统

请按知识产权商标管理系统的一线业务来组织实现，先把岗位、对象、状态和异常讲清楚，再让页面、接口和数据表围绕这些事实展开。
商标管理要覆盖检索评估、申请材料、官方流程、驳回复审、续展和客户提醒。
核心数据对象包括商标、申请人、类别、材料、官方状态、驳回记录、续展任务；关键规则要落到近似检索、类别校验、期限提醒、案件进度跟踪，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 顾问录入商标和类别，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 系统做近似风险提示，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 客户补充申请材料，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 提交官方申请并跟踪状态，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 驳回时发起复审流程，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 注册成功后进入续展提醒，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 管理端查看案件收入和期限，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖近似冲突、材料缺失、官方驳回、期限错过、客户变更、续展遗漏，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 269 专利案件管理系统

专利案件管理系统不能做成通用后台换皮，首版要直接服务真实使用者，把核心流转、规则计算、异常分支和统计口径放进同一套实现。
专利案件管理要围绕交底书、撰写、审核、提交、审查意见、费用和年费维护。
核心数据对象包括专利案件、发明人、交底书、撰写稿、审查意见、费用、年费任务；关键规则要落到期限计算、稿件版本、OA答复跟踪、年费提醒，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 客户提交技术交底书，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 代理人撰写申请文件，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 内部质检审核，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 提交专利局并记录申请号，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 收到审查意见生成答复任务，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 缴费和年费自动提醒，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 案件归档形成知识库，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖交底不完整、期限临近、审查意见逾期、费用未缴、版本错发、发明人变更，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 270 科研期刊投稿审稿系统

落到科研期刊投稿审稿系统这个项目，复杂度应该来自业务本身：谁发起、谁处理、系统如何校验、结果如何归档，都要能在代码里找到依据。
期刊投稿系统要覆盖作者投稿、格式初审、同行评审、修改、录用、排版和发表。
核心数据对象包括稿件、作者、审稿人、审稿意见、修回版本、录用决定、排期；关键规则要落到审稿分派、匿名评审、版本追踪、录用决策，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 作者提交稿件和伦理声明，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 编辑部进行格式初审，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 系统匹配审稿人并发送邀请，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 审稿人提交意见，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 作者修回稿件，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 主编做录用或退稿决定，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 排版后发布和归档，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖审稿人拒审、利益冲突、逾期评审、版本混乱、伦理材料缺失、撤稿，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 271 学术会议征稿系统

生成学术会议征稿系统时，请把前端操作、后端校验、数据库状态和报表来源对齐，避免页面能点但业务事实不可追溯。
会议征稿要支持会议专题、投稿、评审、录用、注册缴费、日程排布和论文集管理。
核心数据对象包括会议、专题、投稿、作者、评审意见、录用结果、注册费、日程；关键规则要落到专题匹配、评审分派、录用统计、日程冲突检测，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 组委会配置会议专题，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 作者提交论文摘要或全文，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 系统分派评审专家，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 专家评分和意见回收，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 组委会确定录用名单，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 作者注册缴费，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 系统排布报告日程和论文集，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖一稿多投、评审逾期、专家冲突、缴费失败、日程冲突、录用变更，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 272 农业合作社社员管理系统

请按农业合作社社员管理系统的一线业务来组织实现，先把岗位、对象、状态和异常讲清楚，再让页面、接口和数据表围绕这些事实展开。
合作社社员管理要围绕入社、土地/资产、生产任务、收益分配、服务补贴和退社处理。
核心数据对象包括社员、土地、股金、生产任务、服务记录、收益分红、补贴、退社申请；关键规则要落到社员资格、土地面积核算、分红计算、补贴发放，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 社员提交入社资料和股金，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 合作社登记土地和种植品类，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 分配生产任务和农资服务，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 记录产量和销售收益，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 按规则计算分红补贴，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 社员查看明细，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 退社时结清权益，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖资格争议、土地面积变更、产量漏报、分红争议、补贴错发、退社清算，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 273 农产品收购结算系统

农产品收购结算系统不能做成通用后台换皮，首版要直接服务真实使用者，把核心流转、规则计算、异常分支和统计口径放进同一套实现。
农产品收购结算要把预约送货、过磅、质检定级、扣杂、结算和付款打通。
核心数据对象包括农户、收购批次、过磅单、质检等级、扣杂记录、结算单、付款流水；关键规则要落到重量净重计算、等级定价、扣杂扣水、付款清分，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 农户预约送货，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 收购点扫码登记批次，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 过磅设备回传毛重皮重，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 质检员录入等级和水分，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 系统按等级扣杂生成结算单，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 财务付款到农户账户，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 管理端统计收购成本，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖重量争议、等级争议、含水超标、重复批次、付款失败、价格临时调整，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 274 温室大棚环境控制系统

落到温室大棚环境控制系统这个项目，复杂度应该来自业务本身：谁发起、谁处理、系统如何校验、结果如何归档，都要能在代码里找到依据。
温室控制系统要围绕传感器监测、阈值策略、设备联动、作物生长期和异常告警展开。
核心数据对象包括大棚、作物、生长期、传感器、控制设备、策略、告警、执行记录；关键规则要落到环境阈值、设备联动、能耗优化、异常告警，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 管理员配置作物和生长期阈值，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 传感器上报温湿光CO2，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 系统判断是否需要通风灌溉补光，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 下发设备控制指令，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 记录执行结果和能耗，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 异常告警推送值班人员，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 复盘产量和环境曲线，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖传感器离线、设备拒动、阈值冲突、极端天气、手动覆盖、能耗过高，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 275 海关申报单证系统

生成海关申报单证系统时，请把前端操作、后端校验、数据库状态和报表来源对齐，避免页面能点但业务事实不可追溯。
海关申报系统要围绕贸易订单、商品归类、单证、申报、查验、税费和放行状态。
核心数据对象包括订单、商品HS编码、发票箱单、报关单、查验记录、税费、放行单；关键规则要落到HS归类校验、单证一致性、税费计算、查验跟踪，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 业务导入贸易订单，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 系统校验商品和HS编码，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 生成发票箱单和报关单，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 报关员提交海关申报，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 查验或补资料进入处理，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 税费缴纳后放行，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 归档全套单证，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖HS归类错误、单证不一致、查验延误、税费差异、资料补正、放行失败，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 276 跨境贸易合规筛查系统

请按跨境贸易合规筛查系统的一线业务来组织实现，先把岗位、对象、状态和异常讲清楚，再让页面、接口和数据表围绕这些事实展开。
跨境合规筛查要覆盖交易方、商品、目的国、制裁名单、出口管制和风险审批。
核心数据对象包括交易方、商品、国家地区、名单库、筛查结果、审批记录、放行结论；关键规则要落到名单匹配、管制品识别、国家风险评分、审批放行，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 业务提交交易和商品信息，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 系统匹配制裁名单和PEP，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 根据HS编码识别管制品，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 计算目的国风险，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 高风险进入合规审批，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 放行或拦截同步业务系统，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 审计查看筛查证据，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖名单模糊匹配、商品归类不准、国家禁运、审批超时、误拦截、名单更新滞后，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 277 仓库自动分拣控制系统

仓库自动分拣控制系统不能做成通用后台换皮，首版要直接服务真实使用者，把核心流转、规则计算、异常分支和统计口径放进同一套实现。
自动分拣控制要把入库包裹、扫码识别、分拣口、设备状态、异常件和产能统计连起来。
核心数据对象包括包裹、条码、分拣任务、格口、设备、异常件、产能记录；关键规则要落到条码识别、路径分配、设备联动、异常件回流，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 包裹进入输送线扫码，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 系统识别目的地和格口，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 控制设备分拣到对应出口，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 未识别包裹进入异常口，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 操作员补码回流，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 设备状态实时监控，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 管理端查看吞吐和错分率，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖条码破损、格口满载、设备停机、错分、重复扫码、异常件堆积，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 278 智能售货机管理系统

落到智能售货机管理系统这个项目，复杂度应该来自业务本身：谁发起、谁处理、系统如何校验、结果如何归档，都要能在代码里找到依据。
售货机管理要覆盖设备点位、商品补货、远程价格、支付出货、故障告警和运营报表。
核心数据对象包括设备、点位、货道、商品、库存、支付订单、故障、补货任务；关键规则要落到货道库存、支付出货、远程调价、故障告警，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 运营维护设备点位和货道，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 补货员扫码补货，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 用户扫码支付购买，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 设备出货结果回传，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 出货失败自动退款，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 缺货或故障生成任务，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 后台分析点位收益，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖卡货、支付成功未出货、设备离线、缺货、价格同步失败、补货差异，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 279 智能门锁公寓管理系统

生成智能门锁公寓管理系统时，请把前端操作、后端校验、数据库状态和报表来源对齐，避免页面能点但业务事实不可追溯。
智能门锁公寓系统要把房源、租客、合同、门锁授权、水电账单和退租交接连起来。
核心数据对象包括房源、房间、租客、合同、门锁密码、账单、维修、退租单；关键规则要落到门锁授权、合同周期、账单计费、退租验房，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 运营维护房源和房间，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 租客签约并缴纳押金，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 系统按合同生成门锁密码，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 租期内生成水电租金账单，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 租客报修关联房间，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 退租时验房扣费，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 门锁权限自动失效，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖密码泄露、合同延期、欠费、门锁离线、维修延误、押金争议，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 280 嵌入式设备 OTA 升级平台

请按嵌入式设备 OTA 升级平台的一线业务来组织实现，先把岗位、对象、状态和异常讲清楚，再让页面、接口和数据表围绕这些事实展开。
OTA平台要覆盖设备分组、固件包、灰度策略、升级任务、回滚和失败诊断。
核心数据对象包括设备、固件、版本、分组、升级任务、升级日志、回滚记录；关键规则要落到版本兼容、灰度发布、断点续传、失败回滚，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 研发上传固件和版本说明，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 运营选择设备分组灰度，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 设备拉取升级任务，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 平台跟踪下载安装进度，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 失败设备进入重试或回滚，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 全量发布后形成版本报表，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 诊断日志辅助定位问题，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖固件不兼容、升级断电、设备离线、灰度失败、回滚异常、版本碎片化，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 281 车载诊断 OBD 数据平台

车载诊断 OBD 数据平台不能做成通用后台换皮，首版要直接服务真实使用者，把核心流转、规则计算、异常分支和统计口径放进同一套实现。
OBD数据平台要围绕车辆接入、故障码、行驶数据、维保建议和车队风险分析展开。
核心数据对象包括车辆、OBD设备、故障码、行驶轨迹、油耗、维保建议、告警；关键规则要落到故障码解析、驾驶行为评分、油耗分析、维保预测，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 车辆绑定OBD设备，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 设备上报故障码和行驶数据，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 系统解析故障等级，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 车主查看维保建议，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 车队管理查看驾驶评分，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 异常告警推送维修，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 历史数据形成健康报告，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖设备离线、故障码误报、数据漂移、隐私轨迹、维保建议争议、车辆换绑，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 282 智能穿戴健康数据平台

落到智能穿戴健康数据平台这个项目，复杂度应该来自业务本身：谁发起、谁处理、系统如何校验、结果如何归档，都要能在代码里找到依据。
穿戴健康平台要处理设备授权、连续体征、睡眠运动分析、异常提醒和医生/家属共享。
核心数据对象包括用户、设备、心率、睡眠、运动、血氧、告警、共享授权；关键规则要落到数据清洗、异常识别、趋势分析、授权共享，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 用户绑定穿戴设备，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 平台同步心率睡眠运动，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 系统清洗异常点并生成趋势，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 异常血氧心率推送提醒，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 用户授权医生或家属查看，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 生成周/月健康报告，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 撤销授权后停止共享，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖设备断连、数据异常、误报警、授权过期、隐私导出、报告口径争议，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 283 智慧校园宿舍管理系统

生成智慧校园宿舍管理系统时，请把前端操作、后端校验、数据库状态和报表来源对齐，避免页面能点但业务事实不可追溯。
宿舍管理要覆盖床位分配、入住退宿、门禁、报修、查寝和水电费用。
核心数据对象包括学生、宿舍楼、房间、床位、门禁、报修、查寝、水电账单；关键规则要落到床位分配、门禁权限、查寝统计、水电分摊，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 辅导员导入学生名单，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 系统按规则分配床位，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 学生办理入住开通门禁，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 日常报修关联房间，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 查寝记录异常学生，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 水电账单按房间分摊，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 退宿时关闭权限和结算，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖调宿、晚归、门禁异常、维修超时、费用争议、毕业批量退宿，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 284 校园教务排课系统

请按校园教务排课系统的一线业务来组织实现，先把岗位、对象、状态和异常讲清楚，再让页面、接口和数据表围绕这些事实展开。
教务排课要把课程、教师、班级、教室、时间片和冲突检测作为核心。
核心数据对象包括课程、教师、班级、教室、时间片、排课任务、调课申请；关键规则要落到冲突检测、教室容量、教师工作量、调课审批，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 教务导入教学计划，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 配置教师和班级约束，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 系统自动排课并检查冲突，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 教务人工调整特殊课程，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 发布课表给师生，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 调课申请走审批，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 统计教室利用率和教师课时，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖教师冲突、教室容量不足、合班课程、临时调课、节假日停课、课表版本变化，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 285 图书馆借阅管理系统

图书馆借阅管理系统不能做成通用后台换皮，首版要直接服务真实使用者，把核心流转、规则计算、异常分支和统计口径放进同一套实现。
借阅管理要覆盖馆藏、读者、借还、预约、逾期罚金和盘点，适合学校或公共图书馆。
核心数据对象包括馆藏、读者、借阅单、预约、罚金、盘点记录、遗失处理；关键规则要落到借阅资格、预约排队、逾期计费、盘点差异，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 馆员维护图书和库存，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 读者检索预约，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 借书时校验可借数量，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 还书更新库存和罚金，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 逾期自动提醒，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 馆员盘点处理遗失，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 报表统计借阅排行，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖图书遗失、逾期未还、预约过期、读者停权、罚金减免、盘点差异，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 286 博物馆藏品管理系统

落到博物馆藏品管理系统这个项目，复杂度应该来自业务本身：谁发起、谁处理、系统如何校验、结果如何归档，都要能在代码里找到依据。
藏品管理要覆盖入藏、编目、库房位置、修复、借展、盘点和数字档案。
核心数据对象包括藏品、入藏记录、编目、库房、修复记录、借展单、盘点、影像档案；关键规则要落到藏品编号、库位追踪、借展审批、修复履历，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 馆员登记入藏来源，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 编目填写材质年代和图片，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 分配库房库位，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 定期盘点核对状态，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 修复部门记录处理过程，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 外借展览走审批和保险，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 数字档案供研究检索，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖藏品损坏、库位变更、借展逾期、影像缺失、盘点不符、权限泄露，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 287 影楼摄影订单管理系统

生成影楼摄影订单管理系统时，请把前端操作、后端校验、数据库状态和报表来源对齐，避免页面能点但业务事实不可追溯。
影楼订单要覆盖咨询、套餐、预约拍摄、选片、精修、交付和售后重修。
核心数据对象包括客户、套餐、预约档期、拍摄任务、原片、选片、精修单、交付件；关键规则要落到档期锁定、选片加购、精修流转、交付验收，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 客户选择套餐并付订金，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 门店预约摄影师和棚位，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 拍摄当天签到执行，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 客户在线选片和加购，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 修图师按选片精修，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 客户验收并下载成品，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 售后重修关联原订单，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖档期冲突、客户改期、选片争议、精修返工、成品丢失、退款纠纷，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 288 宠物寄养酒店管理系统

请按宠物寄养酒店管理系统的一线业务来组织实现，先把岗位、对象、状态和异常讲清楚，再让页面、接口和数据表围绕这些事实展开。
宠物寄养酒店要围绕房型、入住评估、喂养护理、视频探视、加购服务和离店结算。
核心数据对象包括宠物、主人、房间、入住单、喂养计划、护理记录、视频、结算单；关键规则要落到房间容量、健康评估、喂养提醒、离店结算，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 主人提交宠物信息和疫苗证明，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 系统分配适合房型，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 入住时做健康评估，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 员工按计划喂养和清洁，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 主人查看视频和日报，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 加购洗护服务，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 离店结算并评价，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖疫苗过期、宠物生病、房间满员、加购争议、视频离线、提前离店，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 289 社区养老助餐系统

社区养老助餐系统不能做成通用后台换皮，首版要直接服务真实使用者，把核心流转、规则计算、异常分支和统计口径放进同一套实现。
养老助餐要把老人档案、套餐订餐、补贴资格、配送或堂食核销和结算补贴做成闭环。
核心数据对象包括老人档案、补贴资格、菜单、订单、配送、核销、补贴结算；关键规则要落到补贴资格校验、订餐截止、配送路线、核销结算，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 社区维护老人和补贴资格，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 食堂发布每日菜单，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 老人或家属订餐，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 系统计算补贴后价格，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 配送员送餐或堂食核销，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 财务按核销结算补贴，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 管理端查看用餐覆盖率，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖资格过期、临时停餐、配送失败、重复补贴、老人退餐、结算差异，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 290 殡葬服务预约管理系统

落到殡葬服务预约管理系统这个项目，复杂度应该来自业务本身：谁发起、谁处理、系统如何校验、结果如何归档，都要能在代码里找到依据。
殡葬服务预约要尊重业务敏感性，覆盖服务套餐、场地档期、车辆、用品、仪式流程和费用确认。
核心数据对象包括逝者档案、家属、服务套餐、场地、车辆、用品、仪式安排、费用单；关键规则要落到档期资源、套餐计价、流程排程、费用确认，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 工作人员录入逝者和家属信息，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 家属选择服务套餐和仪式时间，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 系统锁定礼厅车辆和人员，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 用品清单生成费用单，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 仪式流程按节点执行，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 家属确认服务和结算，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 档案归档供后续查询，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖档期冲突、信息录错、临时加项、费用争议、用品缺货、隐私保护，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 291 二维码防伪溯源系统

生成二维码防伪溯源系统时，请把前端操作、后端校验、数据库状态和报表来源对齐，避免页面能点但业务事实不可追溯。
防伪溯源系统要围绕批次赋码、生产绑定、流通扫码、消费者验真和异常窜货处理。
核心数据对象包括产品批次、二维码、生产线、经销商、扫码记录、窜货预警、召回单；关键规则要落到唯一码生成、扫码验真、流向追踪、窜货识别，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 品牌方创建产品批次，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 系统生成唯一码并绑定生产信息，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 经销商扫码入出库记录流向，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 消费者扫码查看真伪和溯源，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 异地异常扫码触发窜货预警，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 质量问题按批次召回，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 管理端查看渠道热力图，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖码被复制、批次错绑、离线扫码、窜货争议、召回范围、渠道隐私，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 292 会员储值卡系统

请按会员储值卡系统的一线业务来组织实现，先把岗位、对象、状态和异常讲清楚，再让页面、接口和数据表围绕这些事实展开。
会员储值卡要覆盖开卡、充值、消费、赠送金、退款、挂失和门店对账。
核心数据对象包括会员、储值卡、充值流水、消费流水、赠送规则、退款、挂失记录；关键规则要落到余额账本、赠送金规则、消费扣款、门店日结，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 门店为会员开卡，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 用户充值并获得赠送金，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 消费时按规则扣本金赠送金，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 会员查询余额和流水，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 挂失冻结卡片，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 退款按比例退回，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 财务按门店日结对账，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖重复扣款、赠送金退还、挂失找回、跨店消费、余额不平、退款争议，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 293 洗车美容门店管理系统

洗车美容门店管理系统不能做成通用后台换皮，首版要直接服务真实使用者，把核心流转、规则计算、异常分支和统计口径放进同一套实现。
洗车美容门店要围绕服务项目、预约排队、车辆到店、施工工位、会员套餐和员工提成。
核心数据对象包括车辆、客户、服务项目、预约、工位、施工单、会员套餐、提成；关键规则要落到排队叫号、套餐核销、工位调度、提成计算，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 门店配置洗车美容项目，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 客户预约或到店排队，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 前台识别车辆和会员套餐，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 系统分配工位和技师，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 技师上传施工前后照片，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 完工核销套餐或收款，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 店长查看工位效率和提成，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖高峰排队、套餐过期、车辆损伤争议、工位占用、员工提成争议、客户爽约，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 294 共享设备租赁系统

落到共享设备租赁系统这个项目，复杂度应该来自业务本身：谁发起、谁处理、系统如何校验、结果如何归档，都要能在代码里找到依据。
共享设备租赁要处理设备投放、扫码租借、计费、归还、故障报修和押金风控。
核心数据对象包括设备、点位、用户、租借订单、押金、计费规则、故障单；关键规则要落到设备状态、扫码开锁、计费结算、押金冻结，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 运营投放设备并维护点位，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 用户扫码租借并冻结押金，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 设备开锁开始计费，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 用户归还到指定点位，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 系统结算费用释放押金，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 故障设备进入维修，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 后台分析周转率，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖设备离线、未归还、计费异常、押金不足、恶意损坏、跨点归还，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 295 企业访客预约与会议室联动系统

生成企业访客预约与会议室联动系统时，请把前端操作、后端校验、数据库状态和报表来源对齐，避免页面能点但业务事实不可追溯。
访客预约与会议室联动要把外来人员、接待人、会议室、门禁、签到和安全审计串起来。
核心数据对象包括访客、接待人、会议室、预约、门禁权限、签到记录、访客证；关键规则要落到会议室占用、访客审批、门禁授权、签到核验，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 员工发起访客预约并选择会议室，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 部门或安保审批访客信息，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 系统生成访客码和门禁时段，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 访客到达前台核验签到，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 会议室自动显示占用，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 离场签退关闭权限，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 安保查看访客记录，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖黑名单访客、证件不符、会议室冲突、访客未离场、临时改期、门禁同步失败，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 296 企业车辆预约派车系统

请按企业车辆预约派车系统的一线业务来组织实现，先把岗位、对象、状态和异常讲清楚，再让页面、接口和数据表围绕这些事实展开。
企业派车要覆盖用车申请、审批、车辆司机调度、出车里程、费用和违章。
核心数据对象包括车辆、司机、用车申请、审批、派车单、里程、费用、违章；关键规则要落到车辆档期、审批额度、派车调度、费用核算，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 员工提交用车申请和路线，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 主管审批用车必要性，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 调度分配车辆司机，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 司机出车记录里程油耗，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 用车人确认完成，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 财务核算费用，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 车辆违章或保养关联任务，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖车辆冲突、司机请假、临时改路线、超范围用车、费用争议、违章滞后，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 297 企业食堂订餐系统

企业食堂订餐系统不能做成通用后台换皮，首版要直接服务真实使用者，把核心流转、规则计算、异常分支和统计口径放进同一套实现。
企业食堂订餐要把菜单、订餐截止、补贴、取餐核销、库存备餐和费用结算连接起来。
核心数据对象包括员工、菜单、餐别、订餐单、补贴规则、取餐码、备餐量、结算单；关键规则要落到订餐截止、补贴计算、备餐预测、取餐核销，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 食堂发布每日菜单和餐别，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 员工在截止前订餐，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 系统计算企业补贴和自付金额，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 后厨按订单备餐，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 员工扫码取餐核销，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 未取餐进入处理，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 财务生成月度结算，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖临时退餐、超量备餐、补贴错算、取餐码转借、窗口拥堵、月结差异，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 298 生产线 Andon 异常呼叫系统

落到生产线 Andon 异常呼叫系统这个项目，复杂度应该来自业务本身：谁发起、谁处理、系统如何校验、结果如何归档，都要能在代码里找到依据。
Andon系统要围绕产线异常呼叫、响应、处理、停线时长和改善闭环，服务车间现场。
核心数据对象包括产线、工位、异常类型、呼叫单、响应人员、停线时长、改善措施；关键规则要落到异常分级、响应计时、升级通知、停线统计，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 操作工在工位触发异常呼叫，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 系统按类型通知班组长或维修，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 响应人员到场确认原因，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 处理过程记录停线时长，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 生产恢复后关闭呼叫，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 质量或工艺问题生成改善项，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 管理层查看Top异常，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖无人响应、重复呼叫、误触发、停线超时、原因归类不准、改善未闭环，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 299 质量追溯批次管理系统

生成质量追溯批次管理系统时，请把前端操作、后端校验、数据库状态和报表来源对齐，避免页面能点但业务事实不可追溯。
质量追溯批次管理要把原料、生产批次、工序检验、库存、发货和召回串成完整链路。
核心数据对象包括原料批次、生产批次、工序、检验记录、库存批次、发货批次、召回单；关键规则要落到批次关联、质量判定、库存冻结、召回追踪，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 原料入库生成批次，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 生产工单消耗原料批次，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 各工序记录检验结果，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 成品入库绑定生产批次，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 发货记录流向客户，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 质量问题冻结相关库存，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 召回按批次追踪客户，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖批次混用、检验不合格、库存错绑、发货追踪缺失、召回范围扩大、记录补录，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。

## 300 设备远程诊断与维修派单系统

请按设备远程诊断与维修派单系统的一线业务来组织实现，先把岗位、对象、状态和异常讲清楚，再让页面、接口和数据表围绕这些事实展开。
远程诊断派单要把设备监测、故障识别、远程诊断、派工、备件、维修反馈和知识库积累结合起来。
核心数据对象包括设备、传感器数据、故障码、诊断结论、维修工单、备件、工程师、知识库；关键规则要落到故障识别、远程诊断、派工匹配、备件预占，并让前端列表、详情、表单、看板和报表全部读取同一套后端状态。
1. 设备持续上报运行数据，前端需要提供清晰入口和必填校验，后端创建对应业务记录、初始状态和操作日志。
2. 系统识别故障码和异常趋势，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
3. 工程师远程查看日志并给出诊断，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
4. 需要现场时自动派单匹配工程师，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
5. 备件库预占所需配件，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
6. 维修完成回传结果，后端根据业务规则推进状态，前端只展示当前角色真实可执行的动作，并同步刷新相关明细。
7. 故障经验沉淀到知识库，系统要沉淀结果、统计口径和可追溯记录，确保后续查询、导出和复盘不依赖临时页面状态。
8. 异常处理必须覆盖设备离线、误诊断、备件缺货、工程师超时、客户拒修、重复故障，每类异常都要有阻断、补偿、人工处理或回滚路径，不能继续按正常流程放行。
9. 验收时至少跑通主流程、异常流程、权限越界、重复提交、历史记录追溯和报表穿透，确保实现不是静态样例。
组件约束：不要生成容器化配置或非必要外部服务，数据库默认优先使用 SQLite 文件数据库（例如 data/app.sqlite）以最快打通完整业务链路；如果业务确实强依赖 MySQL、PostgreSQL、Redis、消息队列或对象存储等特定组件，可以引入，但必须说明依赖原因、配置入口、启动方式和本地降级方案。
端口约束：前后端端口必须优先围绕项目目录名称中的数字本身规划，但端口号必须是 0 到 65535 之间的合法数字。四位项目号例如 xm-8744，端口必须包含 8744，可使用 18744、28744 或 48744 这类合法前后缀组合；五位项目号例如 xm-11132，不能拼成 111321 这类超过 65535 的非法端口，必须取后四位 1132 作为端口前后缀，可使用 11132、21132、11321、11322 等合法端口；若目录数字较短，可使用该数字补位后加 01/02（如 xm-71 使用 7101/7102）。统一写入 .env 或配置文件，并同步 CORS、代理和回调地址，避开 3000、5173、8000、8080 等常见端口。
验收约束：完成代码后必须实际启动后端和前端，打印访问地址，并通过关键页面操作和接口请求验证主业务链路已调通。
前后端启动方式统一使用后台启动，关闭终端不影响程序运行。
不要提问我问题，遇到脚本问题、执行问题、权限问题等都允许你自行解决。
执行过程中，项目内的删除动作不需要经过我同意，可直接处理。
按照需求功能的顺序，每一轮都优先考虑搭建核心功能，再逐步考虑其他功能。
